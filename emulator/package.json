{
  "name": "@ue-tms9900/emulator",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    "./*": "./dist/*.js"
  },
  "scripts": {
    "build": "tsup src/*.ts src/**/*.ts --format esm --dts",
    "dev": "tsup src/*.ts src/**/*.ts --format esm --dts --watch"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
